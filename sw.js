self.addEventListener('install', event => {
    // Hier können Sie Ressourcen cachen
});

self.addEventListener('fetch', event => {
    // Hier können Sie benutzerdefinierte Antworten auf Netzwerkanfragen implementieren
});